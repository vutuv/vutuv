<h1>Login</h1>

<%= form_for @conn, session_path(@conn, :create), [as: :session, class: "pure-form pure-form-stacked"], fn f -> %>
  <%= text_input f, :email, placeholder: "email", class: "form-control" %>
  <%= submit "Log in", class: "pure-button pure-button-primary" %>
<% end %>
