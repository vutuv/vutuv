
  <div class="profile-header">
    <div class="profile-header__info">
      <h1><%= gettext "New job posting tag" %></h1>
    </div>
  </div>
</header>
<%= render Vutuv.LayoutView, "flash.html", assigns %>

<div class="breadcrumbs">
<%= Vutuv.UserHelpers.gen_breadcrumbs([
  gettext("Users"),
  {full_name(@user), user_path(@conn, :show, @user)},
  {gettext("Job postings"), user_job_posting_path(@conn, :index, @user)},
  {@job_posting.title, user_job_posting_path(@conn, :show, @user, @job_posting)},
  {gettext("Job posting tags"), user_job_posting_tag_path(@conn, :index, @user, @job_posting)},
  gettext("New")]) %>
</div>

<div class="card-list">
  <section class="card">
    <%= render "form.html", backlink: user_job_posting_tag_path(@conn, :index, @user, @job_posting), changeset: @changeset,
                            action: user_job_posting_tag_path(@conn, :create, @user, @job_posting) %>
  </section>
  <aside>
    <%= render Vutuv.SharedView, "_ad.html", assigns %>
  </aside>
</div>
