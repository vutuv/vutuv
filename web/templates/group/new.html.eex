<h2>New group</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_group_path(@conn, :create, @user) %>

<%= link "Back", to: user_group_path(@conn, :index, @user) %>
