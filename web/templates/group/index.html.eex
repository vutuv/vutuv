<h2><%= gettext "Groups" %></h2>

<table class="table">
  <thead>
    <tr>
      <th><%= gettext "Name" %></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for group <- @groups do %>
    <tr>
      <td><%= group.name %></td>

      <td class="text-right">
        <%= link "Show", to: user_group_path(@conn, :show, @user, group), class: "btn btn-default btn-xs" %>
        <%= link "Edit", to: user_group_path(@conn, :edit, @user, group), class: "btn btn-default btn-xs" %>
        <%= link "Delete", to: user_group_path(@conn, :delete, @user, group), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "New group", to: user_group_path(@conn, :new, @user) %>
