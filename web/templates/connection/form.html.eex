<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :follower_id, class: "control-label" %>
    <%= text_input f, :follower_id, class: "form-control" %>
    <%= error_tag f, :follower_id %>
  </div>

  <div class="form-group">
    <%= label f, :followee_id, class: "control-label" %>
    <%= text_input f, :followee_id, class: "form-control" %>
    <%= error_tag f, :followee_id %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
