<h2>New URL</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_slug_path(@conn, :create, @user) %>

<%= link "Back", to: user_slug_path(@conn, :index, @user) %>
