<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :connection_id, class: "control-label" %>
    <%= text_input f, :connection_id, class: "form-control" %>
    <%= error_tag f, :connection_id %>
  </div>

  <div class="form-group">
    <%= label f, :group_id, class: "control-label" %>
    <%= text_input f, :group_id, class: "form-control" %>
    <%= error_tag f, :group_id %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
