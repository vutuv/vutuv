</header>

<div class="breadcrumbs">
  <%= gen_breadcrumbs([
    gettext("Users"),
    {full_name(@user), user_path(@conn, :show, @user)},
    {gettext("Emails"), user_email_path(@conn, :index, @user)},
    gettext("New")]) %>
</div>

<div class="card-list">

  <aside>
    <%= render Vutuv.SharedView, "_ad.html" %>
  </aside>
  <section class="card">
    <%= render "form.html", backlink: user_email_path(@conn, :index, @user), changeset: @changeset, action: user_email_path(@conn, :update, @user, @email), edit: true %>
  </section>
</div>
