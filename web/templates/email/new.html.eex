<h2>New email</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_email_path(@conn, :create, @user) %>

<%= link "Back", to: user_email_path(@conn, :index, @user) %>
