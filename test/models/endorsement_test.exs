defmodule Vutuv.EndorsementTest do
  use Vutuv.ModelCase

  alias Vutuv.Endorsement

  # @valid_attrs %{}
  # @invalid_attrs %{}

  # test "changeset with valid attributes" do
  #   changeset = Endorsement.changeset(%Endorsement{}, @valid_attrs)
  #   assert changeset.valid?
  # end

  # test "changeset with invalid attributes" do
  #   changeset = Endorsement.changeset(%Endorsement{}, @invalid_attrs)
  #   refute changeset.valid?
  # end
end
