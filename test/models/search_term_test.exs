defmodule Vutuv.SearchTermTest do
  use Vutuv.ModelCase

  alias Vutuv.SearchTerm

  @valid_attrs %{}
  @invalid_attrs %{}

  # test "changeset with valid attributes" do
  #   changeset = SearchTerm.changeset(%SearchTerm{}, @valid_attrs)
  #   assert changeset.valid?
  # end

  # test "changeset with invalid attributes" do
  #   changeset = SearchTerm.changeset(%SearchTerm{}, @invalid_attrs)
  #   refute changeset.valid?
  # end
end
