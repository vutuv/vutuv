<h1><%= @current_user.full_name %> <%= gettext "email addresses" %></h1>

<table>
  <thead>
    <tr>
      <th><%= gettext "Value" %></th>
      <th><%= gettext "Description" %></th>
      <th><%= gettext "Public" %></th>
      <th><%= gettext "Primary" %></th>
      <th><%= gettext "Verified" %></th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for email_address <- @email_addresses do %>
    <tr>
      <td><%= email_address.value %></td>
      <td><%= email_address.description %></td>
      <td><%= email_address.is_public %></td>
      <td><%= email_address.is_primary %></td>
      <td><%= email_address.verified %></td>

      <td>
        <%= link gettext("Show"), to: Routes.user_email_address_path(@conn, :show, @current_user, email_address) %>
        <%= link gettext("Edit"), to: Routes.user_email_address_path(@conn, :edit, @current_user, email_address) %>
        <%= form_for @conn, Routes.user_email_address_path(@conn, :set_primary, @current_user, email_address), [method: "put"], fn _f -> %>
          <%= submit gettext("Set as primary email address"), class: "link" %>
        <% end %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>
