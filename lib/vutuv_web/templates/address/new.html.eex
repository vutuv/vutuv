<h1>New address</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.user_address_path(@conn, :create, @current_user)) %>

<span><%= link gettext("All addresses"), to: Routes.user_address_path(@conn, :index, @current_user) %></span>
